> git config --global user.name "username"

>git config --global user.email "useremail.com"

> git init [to initialise once we create a folder]

> git config --list [list details]

>git add -A [add untracked files]

> git commit -m "commit message"

>git status  [list untracked files]

> git log

to connect to remote repo
git remote add remote_name remote_path  (remote path copied from remote repo)

git push remote-name branch (git push orgin master)